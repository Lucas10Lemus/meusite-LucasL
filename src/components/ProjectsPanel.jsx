import React from 'react';
import './ProjectsPanel.scss';

const ProjectsPanel = () => {
    return (
        <section id="projects" className="projects-panel">
            <h2 className="projects-title">Meus Projetos</h2>
            <p className="placeholder-text">Em breve, meus projetos mais recentes estarão aqui.</p>
        </section>
    );
};

export default ProjectsPanel;